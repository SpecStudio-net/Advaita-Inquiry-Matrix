# Copy this file to .streamlit/secrets.toml and fill in your values.
# secrets.toml is gitignored — never commit it.
#
# On Streamlit Community Cloud, add these via the app's Settings → Secrets panel.

ANTHROPIC_API_KEY = "sk-ant-..."

# Optional: set which model AIM uses (defaults to claude-opus-4-8)
# AIM_MODEL = "claude-opus-4-8"

# Optional: set the guardrail model (defaults to claude-haiku-4-5)
# AIM_GUARDRAIL_MODEL = "claude-haiku-4-5"
